1.

	TP SimulationBanque --> Pourquoi mon try catch ne fonctionne pas ?

2.

	TP SimulationBanque --> Soit A et B deux classes, B hérite de A, A et B possède
				tous les deux la méthode afficher(). J'instancie un
				objet avec la classe B. Comment forcer l'utilisation
				de la méthode de la classe B plutpot que celle de A ?

3.

	TP AireTriangle --> Qu'est ce qui est le plus simple : Demander 6 informations
				à l'utilisateur (via scanner) ou utiliser un fichier 
				que l'on va lire ligne par ligne ?

4.	
	Me conseilles-tu une structure particulière lors de la création des classes qui vont 
	écrire ou lire des fichiers dans un package repository ?